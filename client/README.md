# Client

React + Vite + Ant Design UI for Wallet Watchlist.

```bash
npm install
npm run dev
```

Opens on http://localhost:5173 and proxies `/api` to the server on `:4000`.

Full setup and architecture notes: see the root [README](../README.md).
